# Hyprland submap activated while WoW window is focused.
# Super+1-9 are deliberately UNDEFINED here so the keystroke passes through
# to WoW. Super+Tab still cycles workspaces (Alt+Tab-style mental model).

submap = wow
bind = SUPER, Tab, workspace, e+1
submap = reset
